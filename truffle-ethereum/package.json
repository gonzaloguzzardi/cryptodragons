{
  "scripts": {
    "compile": "truffle compile",
    "develop": "truffle develop",
    "deploy": "rm -rf build; truffle migrate --reset --network development",
    "ganache-cli": "ganache-cli -p 8545 -m gravity top burden flip student usage spell purchase hundred improve check genre",
    "ganache-cli:dev": "scripts/ganache-cli.sh"
  },
  "dependencies": {
    "ganache-cli": "^6.4.1",
    "loom-truffle-provider": "^0.13.0",
    "openzeppelin-solidity": "^2.2.0",
    "truffle": "^5.0.6",
    "truffle-hdwallet-provider": "^1.0.5",
    "truffle-privatekey-provider": "^1.1.0"
  }
}
