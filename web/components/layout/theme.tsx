import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#ecfcff',
    },
    secondary: {
      main: '#5edfff',
    },
    error: {
      main: '#ef5350',
    },
    background: {
      default: '#b2fcff',
    },
  },
})

export default theme
