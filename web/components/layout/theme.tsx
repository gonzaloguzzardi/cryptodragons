import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#fff',
    },
    secondary: {
      main: '#88f',
    },
    text: {
      primary: '#222',
    },
    error: {
      main: '#f44',
    },
    background: {
      default: '#fff',
    },
  },
})

export default theme
